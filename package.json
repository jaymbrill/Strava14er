{
  "name": "colorado-14er-tracker",
  "version": "1.0.0",
  "description": "Track your Colorado 14er summits via Strava",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon server/index.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "setup": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@mapbox/polyline": "^1.2.1",
    "axios": "^1.6.2",
    "connect-pg-simple": "^9.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
